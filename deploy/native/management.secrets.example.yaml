# Structure only. Never commit the populated management.secrets.yaml.
# Create administrator state with `rust-jav administrator init`; configure
# Jellyfin through the authenticated Settings UI.
password_hash: null
bootstrap_token_hash: null
jellyfin_api_key: null
